const fs = require('fs');
const getNotes =  () => 'Your notes ...';

const addNote = (title, body) => {
	
};

const loadNotes = () => {
	
}
module.exports = {
	getNotes: getNotes,
	addNote: addNote
};
